# SYSTEM DEPENDENCY (not pip-installable): libsecp256k1
#   macOS:    brew install secp256k1
#   debian:   apt install libsecp256k1-dev
# REQUIRED AT RUNTIME: the bot cryptographically verifies each participant's
# returned signature (PSBTManager.validate_returned), which is an EC operation.
# Also required by the test suite. preflight.py self-checks it before launch.
#
# python-bitcointx 1.1.5 still binds the secp256k1_ec_privkey_* names that were
# renamed in libsecp256k1 0.6+; src/secp256k1_compat.py bridges this at runtime
# (conftest.py imports the same shim for tests). Delete that module if/when
# bitcointx ships a release with the renamed names.

nostrbot-sdk[lnurl] @ git+https://github.com/unsaltedbutter-ai/nostrbot-sdk.git@v0.5.1
# Pinned: the conftest.py libsecp256k1 symbol-rename shim is written against
# 1.1.5's loader internals. A 1.2+ release could change those and break tests.
python-bitcointx==1.1.5
httpx>=0.28.0
aiosqlite>=0.20.0
python-dotenv>=1.0.0

# Test-time only
pytest>=8.0
pytest-asyncio>=0.23
respx>=0.22
