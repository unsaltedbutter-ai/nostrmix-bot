# SYSTEM DEPENDENCY (not pip-installable): libsecp256k1
#   macOS:    brew install secp256k1
#   debian:   apt install libsecp256k1-dev
# Required by the test suite (real ECDSA-signed PSBTs round-tripped through
# combine + finalize). The bot's runtime does NOT need it — participants
# sign in their own wallets — but pytest will hard-fail without it.
#
# python-bitcointx 1.1.5 still calls the secp256k1_ec_privkey_* names that
# were renamed in libsecp256k1 0.6+. conftest.py at the project root
# bridges this; if/when bitcointx ships a release with the renamed names,
# the conftest.py shim can be deleted.

nostrbot-sdk[lnurl] @ git+https://github.com/unsaltedbutter-ai/nostrbot-sdk.git@v0.4.0
python-bitcointx>=1.1.0
httpx>=0.28.0
aiosqlite>=0.20.0
python-dotenv>=1.0.0

# Test-time only
pytest>=8.0
pytest-asyncio>=0.23
respx>=0.22
