[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "."]
asyncio_mode = "auto"
markers = [
    "live: test hits the live mempool.space API (needs internet). Exclude with -m 'not live' for a hermetic offline run.",
]
